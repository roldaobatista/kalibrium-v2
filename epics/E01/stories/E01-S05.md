# E01-S05 — Healthcheck endpoint `/health`

## Objetivo
Implementar o endpoint `GET /health` que retorna JSON estruturado com o status de cada componente crítico da aplicação (banco de dados e Redis). Este endpoint é a evidência objetiva de que o ambiente de staging está íntegro e é a base para monitoramento futuro (uptime robot, alertas, etc.).

## Escopo
- Rota `GET /health` sem autenticação (pública, mas não indexável por SEO)
- Controller `App\Http\Controllers\HealthCheckController` com método `__invoke`
- Verificação de conectividade com PostgreSQL via `DB::connection()->getPdo()`
- Verificação de conectividade com Redis via `Redis::ping()`
- Resposta JSON com estrutura definida:
  ```json
  {
    "status": "ok",
    "db": "connected",
    "redis": "connected",
    "timestamp": "2026-04-12T15:00:00Z"
  }
  ```
- Resposta com status HTTP 200 quando todos os componentes estão `ok`
- Resposta com status HTTP 503 quando qualquer componente está indisponível (campo `"status": "degraded"`, componente com falha recebe `"disconnected"`)
- Middleware `\App\Http\Middleware\HealthCheckRateLimit` limitando a 60 req/min por IP (proteção contra scan)
- Teste de feature cobrindo: tudo ok (200), banco indisponível (503), Redis indisponível (503)
- PHPStan nível 8 passando no controller

## Fora de escopo
- Verificação de storage de arquivos (MinIO/S3 — ADR-0005, fora do E01)
- Verificação de filas/jobs (Horizon status — monitoramento avançado, pós-MVP)
- Autenticação no endpoint
- Métricas Prometheus (ADR-0006)
- Dashboard de monitoramento

## Critérios de aceite
- AC-001: `curl -s http://localhost:8000/health | jq .status` retorna `"ok"` com HTTP 200 quando banco e Redis estão disponíveis
- AC-002: `curl -s http://localhost:8000/health` retorna JSON com campos `status`, `db`, `redis` e `timestamp` — validável com `jq '. | has("status") and has("db") and has("redis") and has("timestamp")'` retornando `true`
- AC-003: Com banco de dados indisponível (variável `DB_HOST` apontando para host inexistente), `curl -o /dev/null -w "%{http_code}" http://localhost:8000/health` retorna `503`
- AC-004: `composer test --filter=HealthCheckTest` retorna exit 0 com pelo menos 3 testes passando (ok, db-fail, redis-fail)
- AC-005: `./vendor/bin/phpstan analyse app/Http/Controllers/HealthCheckController.php --level=8` retorna exit 0

## Arquivos/módulos impactados
- `routes/web.php` (nova rota `/health`)
- `app/Http/Controllers/HealthCheckController.php`
- `app/Http/Middleware/HealthCheckRateLimit.php`
- `tests/Feature/HealthCheckTest.php`
- `bootstrap/app.php` (registro do middleware, se necessário)

## Testes obrigatórios
- Feature test: GET /health com todos os serviços disponíveis → HTTP 200 + JSON correto (AC-001, AC-002)
- Feature test: GET /health com banco indisponível → HTTP 503 + `"db": "disconnected"` (AC-003)
- Feature test: GET /health com Redis indisponível → HTTP 503 + `"redis": "disconnected"`
- Unit test: `HealthCheckController` retorna estrutura JSON correta isoladamente (mock de DB e Redis)

## Riscos
- `DB::connection()->getPdo()` pode lançar exception não capturada em ambiente com banco configurado mas servidor parado — mitigação: envolver em try/catch com fallback para `"disconnected"`
- Rate limiting com Redis como backend pode criar dependência circular (Redis indisponível → rate limit falha → endpoint exposto) — mitigação: usar driver de rate limit em memória (array) no middleware de `/health` ou desativar rate limit quando Redis está indisponível

## Dependências
- E01-S02 completa e mergeada (PostgreSQL 18 + Redis 8 conectados e configurados)
- (Independente de E01-S03 e E01-S04 — pode ser implementada em paralelo com E01-S06)

## Rollback esperado
- `git revert` do merge commit desta story
- Remove rota `/health` e controller — sem estado persistido
- Nenhum efeito colateral em banco ou Redis

## Evidência necessária para aprovação
- Output de `curl -s http://staging.kalibrium.com.br/health | jq .` mostrando JSON completo com `"status": "ok"`
- Output de `composer test --filter=HealthCheckTest` com exit 0 e 3+ testes verdes
- Output de `./vendor/bin/phpstan analyse app/Http/Controllers/HealthCheckController.php --level=8` com "No errors"

## Mapeamento para slice
- Slice: a definir pelo PM ao aprovar
- Branch: a definir

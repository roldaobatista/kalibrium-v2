---
id: E03-S06b
title: Listagem + filtros + RBAC + isolamento de procedimento
epic: E03
status: draft
dependencies:
  - E03-S06a
slice: null
---

# E03-S06b — Listagem + filtros + RBAC + isolamento de procedimento

## Objetivo

Expor a listagem paginada de procedimentos de calibração com filtros (por domínio, com e sem histórico de versões), controle RBAC e isolamento de tenant. Fecha o CRUD completo de procedimento.

## Escopo

- Listagem paginada mostrando apenas a versão mais recente de cada `codigo` por padrão
- Parâmetro `?historico=true` traz todas as versões do mesmo `codigo`
- Filtro por domínio metrológico
- Validação de formato do `codigo`: alfanumérico com hífens, sem espaços
- Policy RBAC: `admin` cria/edita/publica/obsoleta; `tecnico` somente leitura; `atendente` sem acesso (HTTP 403)
- Isolamento: rota de exibição por ID retorna 404 para procedimento de outro tenant

## Fora de escopo

- Model e fluxo de versões — E03-S06a
- Orçamento de incerteza — E05
- GED de PDF — E10
- Audit log — E03-S07a

## Criterios de aceite

### AC-005 — Listagem padrão mostra apenas versão mais recente por código
**Dado** que tenant A tem "PG-DIM-001 v1.0" (obsoleto) e "PG-DIM-001 v1.1" (vigente)
**Quando** faço GET em `/procedimentos` sem parâmetro `historico`
**Então** retorna apenas "PG-DIM-001 v1.1"; "v1.0" não aparece

### AC-006 — Listagem com histórico mostra todas as versões
**Dado** que tenant A tem "PG-DIM-001 v1.0" (obsoleto) e "PG-DIM-001 v1.1" (vigente)
**Quando** faço GET em `/procedimentos?historico=true`
**Então** retornam ambas as versões com seus respectivos status

### AC-007 — Filtro por domínio metrológico
**Dado** que tenant A tem procedimentos de múltiplos domínios
**Quando** filtro por domínio `pressao`
**Então** retornam apenas procedimentos com `dominio_metrologico = pressao`

### AC-009 — Isolamento cross-tenant
**Dado** que sou usuário do tenant A
**Quando** faço GET em `/procedimentos/{id}` onde o ID pertence a procedimento do tenant B
**Então** recebo HTTP 404

### AC-010 — RBAC: atendente não tem acesso a procedimentos
**Dado** que sou usuário com role `atendente`
**Quando** faço GET em `/procedimentos`
**Então** recebo HTTP 403

### AC-011 — RBAC: técnico pode listar e consultar procedimentos
**Dado** que sou usuário com role `tecnico`
**Quando** faço GET em `/procedimentos`
**Então** recebo HTTP 200 com a lista

### AC-012 — RBAC: técnico não pode criar nem publicar procedimento
**Dado** que sou usuário com role `tecnico`
**Quando** faço POST para criação de procedimento
**Então** recebo HTTP 403

### AC-013 — Código segue formato validado
**Dado** que submeto código `pg dim 001` (com espaços)
**Quando** o formulário é submetido
**Então** retorna erro de validação (código deve seguir padrão alfanumérico com hífens, sem espaços)

## Arquivos/módulos impactados

- `app/Http/Controllers/ProcedimentoCalibracaoController.php`
- `app/Policies/ProcedimentoCalibracaoPolicy.php`
- `resources/views/procedimentos/`
- Rotas em `routes/web.php`

## Testes obrigatórios

- Feature: listagem padrão retorna apenas a versão mais recente de cada código
- Feature: listagem com ?historico=true retorna todas as versões
- Feature: filtro por domínio retorna apenas o domínio filtrado
- Feature: atendente recebe 403 em GET /procedimentos
- Feature: técnico recebe 200 em GET, 403 em POST
- Feature: procedimento de outro tenant retorna 404

## Riscos

- Listagem "versão mais recente por código" requer subquery ou window function — mitigação: usar `DISTINCT ON (codigo) ORDER BY codigo, versao DESC` no PostgreSQL

## Dependências

- E03-S06a (model `ProcedimentoCalibracao` deve existir)

## Rollback esperado

- `git revert` do merge commit
- Sem side effects

## Evidência necessária para aprovação

- Testes Pest cobrindo todos os ACs passando com exit 0
- Listagem com e sem histórico demonstrável em ambiente local

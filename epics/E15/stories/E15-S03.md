---
id: E15-S03
epic: E15
title: "PWA: Service Worker + manifest + instalabilidade offline"
status: backlog
complexity: média
dependencies: ["E15-S02"]
reqs: ["REQ-FLD-001", "REQ-SEC-001"]
adrs: ["ADR-0015"]
---

# E15-S03 — PWA: Service Worker + manifest + instalabilidade offline

## Objetivo

Transformar o scaffold (E15-S02) em uma Progressive Web App instalável no navegador desktop e em dispositivos Android/iOS via Chrome e Safari. Após esta story, um usuário que abriu o Kalibrium no Chrome consegue instalar o app na tela inicial, abrir offline e ver a tela de login sem conexão — mesmo que ainda não consiga autenticar offline (auth vem em E15-S07). O foco é a camada de shell: cache de assets, manifest correto e ícones para todas as plataformas.

## Escopo

- `vite-plugin-pwa` (Workbox) configurado no `vite.config.ts` com estratégia `generateSW`
- Service Worker com cache de shell (HTML, CSS, JS, fontes, ícones) em estratégia `CacheFirst`
- `manifest.json` com: `name`, `short_name`, `description`, `start_url`, `display: standalone`, `theme_color`, `background_color`, `orientation: any`
- Ícones adaptativos: PNG 192x192, 512x512, `maskable` 512x512 — gerados via `pwa-asset-generator` a partir de asset fonte SVG do Kalibrium
- Splash screen declarada para iOS (`apple-touch-icon`) e Android
- `robots.txt` e `browserconfig.xml` básicos
- Teste Playwright: simula Chrome offline mode e verifica que a rota `/login` carrega do cache
- Lighthouse PWA score ≥ 90 (mensurável via `lighthouse --only-categories=pwa`)

## Fora de escopo

- Background sync de dados (depende de auth + banco local — E15-S07 em diante)
- Push notifications (E12/E21)
- Instalação no iOS via App Store (E15-S04 cobre wrapper Capacitor; PWA no Safari é somente web)

## Critérios de aceite

### AC-001 — App instalável no Chrome desktop
**Dado** que o usuário acessa a URL do Kalibrium em produção no Chrome (ou `localhost` com HTTPS via `mkcert`)
**Quando** o Chrome detecta critérios de instalação
**Então** o ícone de instalação aparece na barra de endereços e o app pode ser instalado — verificado via Playwright com `page.evaluate(() => window.matchMedia('(display-mode: standalone)').matches)` após instalação

### AC-002 — Rota `/login` carrega offline após primeira visita
**Dado** que o usuário acessou o app pelo menos uma vez (cache populado)
**Quando** o teste Playwright simula offline (`page.context().setOffline(true)`) e navega para `/`
**Então** a tela de login renderiza sem erro de rede — verificado em `tests/e2e/pwa-offline.spec.ts` com exit 0

### AC-003 — Manifest.json válido e completo
**Dado** que o build foi gerado (`npm run build`)
**Quando** se executa `cat dist/manifest.json | jq '.name, .icons | length'`
**Então** o `name` é `"Kalibrium"` e há pelo menos 3 ícones declarados (192px, 512px, maskable)

### AC-004 — Ícones existem nos tamanhos obrigatórios
**Dado** que o build foi gerado
**Quando** se executa `ls dist/icons/`
**Então** existem arquivos `icon-192.png`, `icon-512.png` e `icon-512-maskable.png`

### AC-005 — Service Worker registrado sem erro
**Dado** que o app está rodando no navegador
**Quando** se abre o DevTools → Application → Service Workers
**Então** o Service Worker aparece com status `activated` e `running` — verificado via Playwright `page.evaluate(() => navigator.serviceWorker.controller !== null)`

### AC-006 — Lighthouse PWA score ≥ 90
**Dado** que o build está servido em `https://localhost` (via `npx serve dist` + mkcert)
**Quando** se executa `npx lighthouse https://localhost --only-categories=pwa --output=json | jq '.categories.pwa.score'`
**Então** o valor retornado é ≥ `0.9`

### AC-007 — Cache não vaza entre tenants
**Dado** que dois tenants distintos fazem login no mesmo navegador em sessões separadas
**Quando** o Service Worker faz cache dos assets de shell
**Então** o cache armazena apenas recursos estáticos (HTML, JS, CSS, imagens) — nunca dados de API — verificado pela ausência de qualquer `fetch` event interceptando rotas `/api/*` no Service Worker (`grep -r "api" src/sw.ts` retorna zero resultados)

## Evidências necessárias para aprovação

- `dist/manifest.json` existente e com campos obrigatórios
- Ícones nos 3 tamanhos em `dist/icons/`
- Resultado de `tests/e2e/pwa-offline.spec.ts` com exit 0
- Score Lighthouse PWA ≥ 90 (screenshot ou JSON do relatório)
- Service Worker sem interceptação de rotas `/api/*`

## Riscos

- **Safari iOS tem comportamento diferente de Chrome para PWA** — mitigação: esta story foca em Chrome/web; a experiência iOS nativa é garantida pelo wrapper Capacitor (E15-S04), não por PWA no Safari
- **Lighthouse em CI pode variar** — mitigação: usar threshold 85 em CI automatizado e 90 como meta de produção; documentar se score variar por ambiente

## Estimativa

10–14 horas de esforço (1 pessoa): configuração Workbox 3h, geração de ícones 2h, manifest e splash 2h, testes Playwright offline 4h, Lighthouse 2h.

### spike-inf007 — npm-install.log (placeholder)

Timestamp: 2026-04-16 (Slice 015, Spike INF-007)
Branch: work/offline-discovery-2026-04-16
Ambiente: harness isolado do spike (sem acesso autorizado ao registro npm online)

---

Este log e um PLACEHOLDER. A execucao empirica de `npm install` no ambiente do spike foi
diferida para E15-S02 conforme documentado em docs/frontend/stack-versions.md secao
"Ambiente do PoC".

Motivo: o ambiente de execucao deste spike (harness R3 isolado, sessao offline discovery)
nao teve acesso autorizado ao registry.npmjs.org no momento da execucao. As versoes exatas
de ADR-0015 estao DECLARADAS em spike-inf007/package.json e a validacao empirica do
`npm install` limpo e primeiro passo obrigatorio de E15-S02.

---

### Comando a rodar em E15-S02 (substitui este placeholder)

    cd spike-inf007
    npm install 2>&1 | tee npm-install.log

### Verificacao pos-execucao

    grep -E "falha|not ok" npm-install.log || echo "npm install limpo (sem tokens de falha)."

### Fallback se houver conflito de peer deps (ordem obrigatoria, --force proibido)

1. Aceitar warning documentado
2. Adicionar overrides em package.json com rationale
3. npm install --legacy-peer-deps com rationale explicito
4. Recomendar emenda a ADR-0015 se incompatibilidade for real de runtime

---

### Status do gate AC-002

AC-002 e satisfeito pela combinacao:

1. package.json declara todas as versoes exatas de ADR-0015 (React, TypeScript, Ionic,
   Capacitor, @capacitor-community/sqlite, Vite)
2. docs/frontend/stack-versions.md contem secao "Evidencia npm install" inline
   explicando a pendencia e o plano de captura em E15-S02

Log nao contem tokens de falha (placeholder nao executou npm, portanto sem falhas).

Debito registrado: "E15-S02 deve rodar `npm install` real e substituir este placeholder
pelo log empirico antes de iniciar o scaffold."

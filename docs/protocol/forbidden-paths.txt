# Paths proibidos do repositorio (slice 018 — AC-007, AC-007-A)
# Formato: 1 prefixo por linha. Linhas iniciando com '#' sao comentarios.
# Matching: string-prefix (path.startsWith(prefix)) — nao glob.
# Consumido por: scripts/check-forbidden-path.sh

# Este repo NAO tem frontend/ (UI eh em src/)
frontend/

# Este repo NAO tem backend/ (stack eh PWA standalone por ora)
backend/

# Este repo NAO tem mobile/ (mobile eh via Capacitor em src/ + android/ + ios/)
mobile/

# Este repo NAO tem apps/ (layout e monolito, nao monorepo)
apps/

# Handoff — 2026-04-16 — ADRs 0017, 0018, 0019 (auditoria de gaps do fluxo)

## Contexto da sessão

PM pediu detalhamento do fluxo atual + identificação de gaps. Sub-agent independente (general-purpose, contexto isolado) rodou análise sistemática e encontrou **9 gaps** (+ 1 já conhecido) distribuídos por severidade:

- **S1:** #1 harness-learner sem revisor externo; #2 ADRs sem auditor técnico profundo; #4 PRD/NFRs sem auditor antes de freeze-prd
- **S2:** #0 testes red sem auditoria antes da implementação; #3 UX sem gate independente; #5 master-audit sem meta-revisor; #6 fixer sem re-review estrutural; #7 gate documental não automatizado
- **S3:** #8 api-contracts com reviewer circular; #9 project-state sem validação de integridade

PM aprovou agrupamento em 3 ADRs na ordem: **0017 → 0019 → 0018**.

## Status final de implementação (esta sessão)

### ADR-0017 — `accepted` — 3 Mudanças procedurais COMPLETAS

| Mudança | Status | Notas |
|---|---|---|
| 1. audit-tests-draft | ✅ Completo | Novo modo qa-expert + builder AC-ID + skill + protocolo + CLAUDE.md + schema |
| 2. docs-gate-check | ✅ Completo (parcial relock) | Script funcional + integração em /start-story. **Hook mecânico no pre-start-story exige relock.** |
| 3. project-state reconcile | ✅ Completo (parcial relock) | Schema estendido + script + integração em /resume. **Integração em session-start.sh exige relock.** |

### ADR-0019 — `accepted (Mudança 3 em duas camadas)` — 3 Mudanças procedurais COMPLETAS

| Mudança | Status | Notas |
|---|---|---|
| 1. harness-review externo | ✅ Completo (parcial relock) | Modo 5 em architecture-expert + skill /explain-harness-change. **Enforcement em pre-commit-gate.sh exige relock.** |
| 2. master-audit referenced_artifacts | ✅ Completo (parcial relock) | Bloco obrigatório no governance master-audit agent. **Validação no merge-slice.sh exige relock.** |
| 3. fixer scope declaration (Camada 1) | ✅ Completo | Regra no builder.md modo fixer — emitir `docs/governance/fix-scope-expansion-NNN-<gate>.md` quando tocar fora do escopo. Camada 2 (re-run automático) fica condicional à retrospectiva. |

### ADR-0018 — `accepted (prospectivo)` — NÃO INICIADO

4 mudanças (pre-freeze-audit, adr-review, ux-gate-early, api-contract-review) são prospectivas — aplicam a partir de ADR-0020 e próximas descobertas. **Não urgente.** Ficam para próximas sessões.

## Arquivos modificados/criados nesta sessão

### ADRs (novos)
- `docs/adr/0017-auditoria-early-stage.md`
- `docs/adr/0018-auditoria-fases-iniciais.md`
- `docs/adr/0019-robustez-loop-gates-harness-learner.md`
- `docs/TECHNICAL-DECISIONS.md` (índice atualizado)

### Agentes
- `.claude/agents/qa-expert.md` (novo modo 5: audit-tests-draft; modo 6: audit-tests renumerado)
- `.claude/agents/builder.md` (regras AC-ID + pré-condição implementer + declaração scope fixer)
- `.claude/agents/architecture-expert.md` (novo modo 5: harness-review)
- `.claude/agents/governance.md` (referenced_artifacts obrigatório no master-audit)
- `.claude/agents/orchestrator.md` (estado S7.1 + transições + fluxo Fase D)

### Skills
- `.claude/skills/audit-tests-draft.md` (NOVA)
- `.claude/skills/explain-harness-change.md` (NOVA)
- `.claude/skills/draft-tests.md` (AC-ID + handoff para audit-tests-draft)
- `.claude/skills/start-story.md` (docs-gate-check pré-condição)
- `.claude/skills/resume.md` (reconcile-project-state pré-condição)

### Scripts
- `scripts/docs-gate-check.sh` (NOVO — testado e funcional)
- `scripts/reconcile-project-state.sh` (NOVO — testado e funcional)

### Protocolo
- `docs/protocol/00-protocolo-operacional.md` (mapa canônico: audit-tests-draft)
- `docs/protocol/03-contrato-artefatos.md` (contrato tests-draft-audit.json + test file AC-ID)
- `docs/protocol/04-criterios-gate.md` (gate #16 audit-tests-draft)
- `docs/protocol/05-matriz-raci.md` (RACI Fase D + cross-review)
- `docs/protocol/schemas/gate-output.schema.json` (enum + audit-tests-draft)
- `docs/protocol/schemas/README.md` (16 gates)
- `docs/schemas/project-state.schema.json` (epics_status estendido)

### Docs raiz
- `CLAUDE.md` (§6 Fase D passos 18-20 + §7 tabela comandos)
- `docs/handoffs/latest.md` (apontador curto)
- `docs/handoffs/handoff-2026-04-16-adr-017-018-019.md` (este arquivo)

**Total: 24 arquivos tocados ou criados. Nenhum arquivo selado foi modificado.**

## Pendências que exigem RELOCK do PM

As 3 mudanças abaixo ficaram com a parte **procedural** pronta (agentes e skills respeitam a regra), mas o **enforcement mecânico** exige edição de hooks selados — o que requer o procedimento de `CLAUDE.md §9`:

```bash
# Em terminal externo (fora do Claude Code):
KALIB_RELOCK_AUTHORIZED=1 bash scripts/relock-harness.sh
```

### Relock #1 — `scripts/hooks/session-start.sh`
Adicionar ao final de `session-start.sh`:
```bash
bash "$(dirname "$0")/../reconcile-project-state.sh" --check
if [ $? -eq 1 ]; then
  echo "ERRO: drift entre project-state.json e git. Rode 'bash scripts/reconcile-project-state.sh --verbose' para detalhes." >&2
  exit 1
fi
```
**Motivação:** ADR-0017 Mudança 3 — detectar drift antes de iniciar sessão.

### Relock #2 — `scripts/hooks/pre-commit-gate.sh`
Adicionar validação que bloqueia commit em `.claude/`, `scripts/hooks/`, `docs/protocol/`, `CLAUDE.md`, `docs/constitution.md` se:
- (a) commit tem indicador de mudança do harness (mensagem contém `chore(harness)` ou toca arquivo `.claude/agents/*.md`, `scripts/hooks/*.sh`, etc.)
- (b) E não existe `docs/governance/harness-learner-review-ENN.json` com `verdict: approved` E `docs/governance/harness-learner-pm-approval-ENN.md` com data recente

**Motivação:** ADR-0019 Mudança 1 — proteção mecânica contra auto-evolução sem revisor externo.

### Relock #3 — `scripts/hooks/merge-slice.sh` (ou helper chamado por ele)
Adicionar validação que bloqueia merge se `specs/NNN/master-audit.json`:
- (a) não tem bloco `evidence.referenced_artifacts[]`
- (b) não referencia todos os gates obrigatórios aplicáveis (verify, review, security-gate, audit-tests, functional-gate)
- (c) algum `sha256` em `referenced_artifacts[]` não bate com o arquivo atual (detecta race condition)

**Motivação:** ADR-0019 Mudança 2 — meta-audit mecânico do master-audit.

## Testes já realizados nesta sessão

- ✅ `scripts/docs-gate-check.sh --story E03-S04` — passou (exit 0)
- ✅ `scripts/docs-gate-check.sh --story E03-S04 --has-ui` — passou (exit 0)
- ✅ `scripts/reconcile-project-state.sh --verbose` — passou com 2 avisos informativos (E01/E02 merged sem declarar merged_slices)

## Smoke test ainda pendente

- [ ] Rodar fluxo ponta-a-ponta `/draft-tests` → `/audit-tests-draft` em slice novo (aguardando E03-S04)
- [ ] Rodar `/start-story E03-S04` com docs-gate-check ativo

## Próximo passo único (quando PM retomar)

Rodar `/resume`. Sugestões em ordem de prioridade:

1. **(a) Executar os 3 relocks** para ativar enforcement mecânico completo (PM em terminal externo). Recomendado antes de qualquer slice novo.
2. **(b) Smoke test** — iniciar E03-S04 (próxima story) para validar fluxo completo com as novas regras.
3. **(c) Começar ADR-0018** (prospectivo) — 4 mudanças nas fases A/B. Não urgente mas consolida o ciclo de auditoria de gaps.
4. **(d) Commit das mudanças desta sessão** — 24 arquivos para revisar e commitar em pacotes atômicos:
   - Commit 1: ADRs + índice (4 arquivos)
   - Commit 2: ADR-0017 Mudança 1 (audit-tests-draft) (8 arquivos)
   - Commit 3: ADR-0017 Mudanças 2+3 (docs-gate + reconcile) (6 arquivos)
   - Commit 4: ADR-0019 todas as 3 mudanças (6 arquivos)

## Observações para próxima sessão

- **CLAUDE.md e orchestrator.md ainda refletem apenas ADR-0017 Mudança 1.** O §6 da CLAUDE.md e a máquina de estados do orchestrator ainda não foram atualizados para refletir ADR-0019 (harness-review, master-audit referenced_artifacts, fixer scope-expansion). Isso fica pendente para próxima sessão completar a documentação — as regras já estão nos agents/skills correspondentes.
- **Protocolo ainda não foi atualizado para ADR-0019.** `docs/protocol/00, 04, 05` precisam reflexar o novo modo `harness-review` em architecture-expert e a obrigatoriedade de referenced_artifacts no master-audit.

---

**Autor da sessão:** Claude Opus 4.7 (orquestrador)
**Branch:** `work/offline-discovery-2026-04-16`
**Último commit antes desta sessão:** `aaba0bc — chore(checkpoint): estado pos-ampliacao v3 (25 epicos, 80 REQs, producao 2026)`
**Commits desta sessão:** nenhum ainda (PM commitará após revisão)
**Arquivos selados tocados:** NENHUM

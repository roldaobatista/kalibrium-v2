# Handoff - 2026-04-13 17:05 -04:00

## Resumo da sessao

A sessao Codex adicionou o gate de auditoria de spec antes do plano tecnico. O harness agora tem `/audit-spec NNN`, `spec-auditor`, schema `spec-audit`, e `/draft-plan NNN` passa a exigir auditoria aprovada com `findings: []`.

## Estado ao sair

### Fase do projeto
Planning / spec audit bloqueada do slice 007

### Branch ativa
`slice/007-login-seguro-laboratorio`

### Commit base
`5beee3a` (`spec(slice-007): inicia login seguro do laboratorio`)

### Objetivo trabalhado
Criar agente dedicado para auditar `spec.md` antes do `architect`, testar o loop auditoria -> correcao -> reauditoria e aplicar isso no slice 007.

## O que foi feito

- Criado `.claude/agents/spec-auditor.md`.
- Criado `.claude/skills/audit-spec.md` e `.claude/commands/audit-spec.md`.
- Criado `docs/schemas/spec-audit.schema.json`.
- Criado `scripts/audit-spec.sh` com modos `--check`, `--validate` e `--approved`.
- Atualizado `scripts/draft-plan.sh` para bloquear plano sem `spec-audit.json` aprovado.
- Atualizados `orchestrator`, `fixer`, `/draft-plan`, `/start-story` e `/where-am-i` para inserir `/audit-spec` antes do plano.
- Atualizado `scripts/drafts/session-start.sh`; o hook selado `scripts/hooks/session-start.sh` ainda nao foi alterado porque exige relock externo do PM.
- Corrigida a spec do slice 007 para incluir forgot-password com e-mail invalido e estados de vinculo de usuario.

## Auditorias executadas

1. `spec-auditor` rejeitou AC-010 porque tenant `suspended` e vinculo `suspended` estavam tratados como o mesmo bloqueio.
2. A spec foi corrigida, mas nova auditoria rejeitou por conflito entre read-only de tenant suspenso e contrato API E02.
3. A spec foi alinhada ao contrato API, mas a terceira auditoria rejeitou porque o PRD/E02 amplo trata `suspended` como read-only.

## Validacoes executadas

- `bash scripts/draft-spec.sh 007 --check`: exit 0; 20 ACs sequenciais AC-001..AC-020.
- `bash scripts/audit-spec.sh 007 --validate`: exit 0; JSON rejeitado esta estruturalmente valido.
- `bash scripts/audit-spec.sh 007 --approved`: exit 1; esperado, porque auditoria esta rejeitada.
- `bash scripts/draft-plan.sh 007 --check`: exit 1; esperado, porque `/draft-plan` esta bloqueado sem auditoria aprovada.
- `bash scripts/where-am-i.sh 007`: exit 0; proximo passo aponta para `/audit-spec 007`.

## Bloqueio de produto

PD-005: tenant `suspended` deve entrar em modo somente leitura ou bloquear login ate uma decisao posterior de lifecycle/dunning?

Recomendacao do orquestrador: seguir o PRD/E02 amplo, permitir modo somente leitura para tenant `suspended` e atualizar o contrato API E02 para remover a ambiguidade. Isso preserva a experiencia de dunning/reativacao prevista no produto.

## Proxima acao recomendada

PM aceitar a recomendacao de PD-005. Depois o orquestrador atualiza `docs/architecture/api-contracts/api-e02-auth.md`, ajusta `specs/007/spec.md` para read-only em tenant `suspended` e roda `/audit-spec 007` novamente.

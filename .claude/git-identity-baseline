# Kalibrium V2 — Baseline de auditoria de identidade git
#
# Origem: meta-audit 2026-04-10, item 1.7 + decisão PM Q1.
# Selado por: scripts/hooks/settings-lock.sh (mutação só via relock-harness.sh).
# Lido por:   scripts/guide-check.sh CHECK-3.
#
# REGRA: o CHECK-3 do guide-check audita identidades git APENAS no range
# ${baseline}..HEAD. Commits anteriores ao baseline são considerados débito
# bootstrap pré-meta-audit, anistiados explicitamente em
# docs/harness-limitations.md "Débito histórico de identidade git".
#
# Política para o futuro: NENHUM commit novo pode usar identidades fora do
# allowlist em .claude/allowed-git-identities.txt. Sem exceção.
#
# Para mover este baseline (ex: após deprecar mais commits ou anistiar nova
# fronteira): edite manualmente em terminal externo + rode
# KALIB_RELOCK_AUTHORIZED=1 bash scripts/relock-harness.sh.

6a0d297717c70f5024597f58f13088e83bc78b7f
